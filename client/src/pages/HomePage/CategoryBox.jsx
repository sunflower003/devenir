import styles from './CategoryBox.module.css';

const CategoryBox = () => {
    return (
        <div className={`${styles.categoryBox} container`}>
            <h1>Category Title</h1>
        </div>
    );
};

export default CategoryBox;